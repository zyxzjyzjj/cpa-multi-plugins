These fixtures preserve only allowlisted model catalogue fields from the official
CodeArts extension 26.9.101 capture (`5555.har`). Request headers, credentials,
cookies, account identifiers, prompts and conversation data are excluded. The
gateway base URL is replaced with a reserved test domain. No HAR file is needed
to run the tests.
